// Control Structures
let i: int = 1;
let sum: int = 0;

while (i <= 10) {
    sum += i;
    i += 1;
}
print(sum);

let res: string[];
i = 1;
while (i <= 20) {
    if (i % 15 == 0) {
        res += List.of("fizzbuzz");
    } elif (i % 3 == 0) {
        res += List.of("fizz");
    } elif (i % 5 == 0) {
        res += List.of("buzz");
    } else {
        res += List.of("?");
    }
    i += 1;
}
print(res);