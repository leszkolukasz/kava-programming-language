fun foo(const x: string): void {
    // Error
    // x = "world";
    print(x + " world");
}

print(foo);
foo("hello");

fun fact(x: int): int {
    if (x == 0) {
        return 1;
    } else {
        return x * fact(x - 1);
    }
}

print(fact(3));
print(fact(5));